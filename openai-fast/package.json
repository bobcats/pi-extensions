{
  "name": "openai-fast-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx --test --test-timeout=5000 *.test.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "tsx": "^4.20.0"
  }
}
